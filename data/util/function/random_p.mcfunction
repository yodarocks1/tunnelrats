#>
# @returns a random number between 0 and 100
#
# In its own function to make inclusion of a sequence or seed easier in the future 

# TODO reactivate this when they fixed the issues with "return run"
# return run random value 0..100